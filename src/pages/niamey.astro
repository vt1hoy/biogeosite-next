---
/**
 * Niamey GIS case page — section order: intro → maps → PDFs → code → workflow → pipeline.
 */
import SiteLayout2 from "../layouts/SiteLayout2.astro";

const pageTranslations = {
  ru: {
    title: "Ниамей - риск затопления",
    subtitle:
      "Пилотный GIS-кейс по анализу риска затопления в Ниамее на основе цифровой модели рельефа (Copernicus DEM): уклон, накопление стока и индекс риска (flow accumulation + slope) для выявления структуры дренажной сети и потенциальных зон подтопления."
  },
  en: {
    title: "Niamey — flood risk",
    subtitle:
      "Pilot GIS case on flood risk analysis in Niamey based on a digital elevation model (Copernicus DEM): slope, flow accumulation and a composite risk index to reveal drainage structure and potential flood-prone areas."
  },
  fr: {
    title: "Niamey — risque d’inondation",
    subtitle:
      "Cas SIG pilote d’analyse du risque d’inondation à Niamey basé sur un modèle numérique de terrain (Copernicus DEM) : pente, accumulation de flux et indice de risque pour identifier la structure du drainage et les zones potentiellement inondables."
  }
};

const images = [
  { src: "/assets/niamey/images/flood_risk_index.png" },
  { src: "/assets/niamey/images/ndwi.png" }
];

const pdfs = [{ file: "/assets/niamey/pdf/brief_niamey2.pdf" }];

const pipelineScripts = [
  { name: "1. niamey_dem_preprocess.py", file: "/assets/niamey/code/niamey_dem_preprocess.py" },
  { name: "2. slope.py", file: "/assets/niamey/code/niamey_slope.py" },
  { name: "3. flow_accumulation.py", file: "/assets/niamey/code/niamey_flow.py" },
  { name: "4. flood_risk_index.py", file: "/assets/niamey/code/niamey_index.py" }
];

const codeFetchTargets = [
  { id: "code1", path: "/assets/niamey/code/niamey_slope.py" },
  { id: "code2", path: "/assets/niamey/code/niamey_flow.py" }
];

const translations = {
  ru: {
    caseLabel: "Кейс",
    caseIntro:
      "Этот кейс представляет пилотный spatial analysis риска затопления в Ниамее на основе цифровой модели рельефа (Copernicus DEM). Анализ включает расчёт уклона, накопления стока (flow accumulation) и построение индекса риска, чтобы выявить структуру дренажной сети и потенциальные зоны подтопления.",
    caseResultsTitle: "Что получилось",
    caseResults: [
      "Выявлена детализированная структура дренажной сети, соответствующая рельефу территории.",
      "Основное русло реки Нигер чётко выделяется как зона максимального накопления стока.",
      "Определены вторичные и локальные каналы, формирующие потенциальные пути распространения воды.",
      "Обнаружены участки с высоким риском затопления вне текущих водных объектов.",
      "Итоговая модель отражает пространственную предрасположенность территории к затоплению."
    ],
    maps: "Карты",
    pdf: "PDF",
    workflow: "Workflow",
    python: "Python / numpy / whitebox",
    pipeline: "Full pipeline",
    pipelineSubtitle: "Полная последовательность скриптов по кейсу",
    pdfOpen: "Открыть PDF",
    fileOpen: "Открыть файл",
    scriptOpen: "Скачать / открыть",
    copy: "Копировать",
    copied: "Скопировано",
    imageTitles: ["flood risk index", "ndwi"],
    pdfTitles: ["Аналитическая записка"],
    workflowItems: [
      {
        title: "Workflow 1 — подготовка данных",
        steps: [
          "Загрузка Copernicus DEM и обрезка по границе Ниамея",
          "Перепроецирование в UTM 28N (EPSG:32628)",
          "Подготовка растра рельефа (DEM)",
          "Проверка корректности пространственных параметров"
        ]
      },
      {
        title: "Workflow 2 — пространственный анализ",
        steps: [
          "Заполнение депрессий рельефа (FillDepressions)",
          "Расчёт накопления стока (D8 Flow Accumulation)",
          "Расчёт уклона (slope)",
          "Логарифмирование flow accumulation (log10)",
          "Нормализация показателей (flow и slope)",
          "Расчёт интегрального индекса риска",
          "Визуализация и оформление карты"
        ]
      }
    ],
    codeTitle1: "numpy - slope",
    codeTitle2: "whitebox — flow accumulation",
    codeDesc1:
      "Расчёт уклона рельефа на основе DEM с использованием градиентов по осям X и Y.",
    codeDesc2:
      "Расчёт накопления стока (D8) на основе DEM, показывающий количество ячеек, дренирующихся в каждую точку",
    codeLoadError: "Ошибка загрузки кода"
  },

  en: {
    caseLabel: "Case",
    caseIntro:
      "This case presents a pilot spatial analysis of flood risk in Niamey based on a digital elevation model (Copernicus DEM). The analysis includes slope calculation, flow accumulation, and a composite risk index to identify drainage structure and potential flood-prone areas.",
    caseResultsTitle: "Key results",
    caseResults: [
      "A detailed drainage network structure derived from terrain has been identified.",
      "The Niger River clearly stands out as the main high flow accumulation corridor.",
      "Secondary and local channels forming potential water pathways have been detected.",
      "Areas with high flood risk outside existing water bodies have been identified.",
      "The final model represents spatial flood susceptibility across the territory."
    ],
    maps: "Maps",
    pdf: "PDF",
    workflow: "Workflow",
    python: "Python / numpy / whitebox",
    pipeline: "Full pipeline",
    pipelineSubtitle: "Complete script sequence used in this case",
    pdfOpen: "Open PDF",
    fileOpen: "Open file",
    scriptOpen: "Download / open",
    copy: "Copy",
    copied: "Copied",
    imageTitles: ["flood risk index", "ndwi"],
    pdfTitles: ["GIS Analysis Brief"],
    workflowItems: [
      {
        title: "Workflow 1 — data preparation",
        steps: [
          "Load Copernicus DEM and clip to Niamey boundary",
          "Reproject to UTM 28N (EPSG:32628)",
          "Prepare elevation raster (DEM)",
          "Validate spatial consistency"
        ]
      },
      {
        title: "Workflow 2 — spatial analysis",
        steps: [
          "Fill depressions in DEM (FillDepressions)",
          "Compute flow accumulation (D8)",
          "Calculate slope",
          "Apply log transformation to flow accumulation (log10)",
          "Normalize variables (flow and slope)",
          "Compute composite flood risk index",
          "Map visualization and styling"
        ]
      }
    ],
    codeTitle1: "numpy — slope",
    codeTitle2: "whitebox - flow accumulation",
    codeDesc1:
      "Slope calculated from DEM using spatial gradients along X and Y directions",
    codeDesc2:
      "D8 flow accumulation computed from DEM, representing the number of upstream cells draining into each pixel",
    codeLoadError: "Code loading error"
  },

  fr: {
    caseLabel: "Cas",
    caseIntro:
      "Ce cas présente une analyse spatiale pilote du risque d’inondation à Niamey basée sur un modèle numérique de terrain (Copernicus DEM). L’analyse inclut le calcul de la pente, de l’accumulation des flux et la construction d’un indice de risque afin d’identifier la structure de drainage et les zones potentiellement inondables.",
    caseResultsTitle: "Résultats clés",
    caseResults: [
      "Une structure détaillée du réseau de drainage dérivée du relief a été identifiée.",
      "Le fleuve Niger apparaît clairement comme le principal corridor d’accumulation des flux.",
      "Des canaux secondaires et locaux formant des voies potentielles d’écoulement ont été détectés.",
      "Des zones à fort risque d’inondation en dehors des cours d’eau actuels ont été identifiées.",
      "Le modèle final représente la susceptibilité spatiale aux inondations du territoire."
    ],
    maps: "Cartes",
    pdf: "PDF",
    workflow: "Workflow",
    python: "Python / numpy / whitebox",
    pipeline: "Full pipeline",
    pipelineSubtitle: "Séquence complète des scripts utilisés dans ce cas",
    pdfOpen: "Ouvrir le PDF",
    fileOpen: "Ouvrir le fichier",
    scriptOpen: "Télécharger / ouvrir",
    copy: "Copier",
    copied: "Copié",
    imageTitles: ["flood risk index", "ndwi"],
    pdfTitles: ["Note d’analyse SIG"],
    workflowItems: [
      {
        title: "Workflow 1 — préparation des données",
        steps: [
          "Chargement du DEM Copernicus et découpage selon la zone de Niamey",
          "Reprojection en UTM 28N (EPSG:32628)",
          "Préparation du raster d’élévation (DEM)",
          "Vérification de la cohérence spatiale"
        ]
      },
      {
        title: "Workflow 2 — analyse spatiale",
        steps: [
          "Remplissage des dépressions (FillDepressions)",
          "Calcul de l’accumulation des flux (D8)",
          "Calcul de la pente",
          "Transformation logarithmique du flow accumulation (log10)",
          "Normalisation des variables (flow et pente)",
          "Calcul de l’indice de risque",
          "Visualisation et mise en forme de la carte"
        ]
      }
    ],
    codeTitle1: "numpy — slope",
    codeTitle2: "whitebox — flow accumulation",
    codeDesc1:
      "Pente calculée à partir du DEM en utilisant les gradients spatiaux selon les axes X et Y",
    codeDesc2:
      "Accumulation des flux (D8) calculée à partir du DEM, représentant le nombre de cellules en amont drainant vers chaque pixel",
    codeLoadError: "Erreur de chargement du code"
  }
};
---

<SiteLayout2 pageTranslations={pageTranslations}>
  <style>
    .block {
      --case-border: #d9e1ea;
      --case-surface: rgba(255, 255, 255, 0.96);
      --case-surface-muted: #fbfdff;
      --case-heading: #1d3d67;
      --case-subheading: #284a73;
      --case-text: #425b78;
      --case-muted: #5d6f86;
      --case-link: #174b87;

      background: var(--case-surface);
      padding: 26px 24px;
      border-radius: 18px;
      margin-bottom: 24px;
      border: 1px solid var(--case-border);
      box-shadow: 0 8px 20px rgba(40, 60, 90, 0.05);
    }

    .block h2 {
      margin: 0 0 18px;
      padding-bottom: 10px;
      border-bottom: 1px solid #e8edf3;
      color: var(--case-heading);
      font-size: 28px;
      letter-spacing: -0.02em;
    }

    .intro-text {
      color: var(--case-text);
      line-height: 1.72;
      font-size: 15px;
      margin-bottom: 18px;
      max-width: 980px;
    }

    .findings-title {
      color: var(--case-subheading);
      margin: 22px 0 12px;
      font-size: 18px;
    }

    .findings-list {
      margin: 0;
      padding-left: 1.25rem;
      color: var(--case-text);
      line-height: 1.8;
      list-style-type: disc;
    }

    .findings-list li {
      margin-bottom: 4px;
    }

    .image-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }

    .image-card {
      background: #fff;
      border: 1px solid #dde5ed;
      border-radius: 14px;
      overflow: hidden;
    }

    .image-card img {
      width: 100%;
      display: block;
      aspect-ratio: 16 / 10;
      object-fit: cover;
    }

    .image-caption {
      padding: 14px 16px;
      font-size: 14px;
      color: var(--case-muted);
    }

    .image-caption a {
      color: var(--case-link);
      font-weight: 600;
    }

    .pdf-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 18px;
    }

    .pdf-card {
      background: var(--case-surface-muted);
      border: 1px solid #e1e8f0;
      border-radius: 14px;
      padding: 18px 20px;
    }

    .pdf-card h3 {
      margin: 0 0 12px;
      color: var(--case-subheading);
      font-size: 18px;
    }

    .pdf-card a {
      color: var(--case-link);
      font-weight: 600;
      font-size: 14px;
    }

    .workflow-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }

    .workflow-item {
      background: var(--case-surface-muted);
      border: 1px solid #e1e8f0;
      border-radius: 14px;
      padding: 18px 20px;
    }

    .workflow-item h3 {
      margin: 0 0 14px;
      color: var(--case-subheading);
      font-size: 18px;
      line-height: 1.35;
    }

    .workflow-item ul {
      margin: 0;
      padding-left: 1.25rem;
      line-height: 1.75;
      color: var(--case-text);
      list-style-type: disc;
    }

    .workflow-item li {
      margin-bottom: 6px;
    }

    .pipeline-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 18px;
    }

    .pipeline-card {
      display: flex;
      flex-direction: column;
      gap: 12px;
      background: var(--case-surface-muted);
      border: 1px solid #e1e8f0;
      border-radius: 14px;
      padding: 18px 20px;
    }

    .script-name {
      margin: 0;
      font-size: 16px;
      font-weight: 700;
      color: var(--case-heading);
      font-family: Consolas, monospace;
      line-height: 1.45;
    }

    .pipeline-card a {
      color: var(--case-link);
      font-weight: 600;
      font-size: 14px;
    }

    .code-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 22px;
    }

    .code-card {
      background: var(--case-surface-muted);
      border: 1px solid #e1e8f0;
      border-radius: 14px;
      padding: 18px 20px;
    }

    .code-card h3 {
      margin: 0 0 12px;
      color: var(--case-subheading);
    }

    .code-description {
      margin-bottom: 14px;
      color: #5b6d85;
      font-size: 14px;
      line-height: 1.6;
    }

    .code-top {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      margin-bottom: 12px;
      flex-wrap: wrap;
    }

    .code-top a {
      color: var(--case-link);
      font-weight: 600;
      font-size: 14px;
    }

    .btn {
      background: var(--case-link);
      color: white;
      border: none;
      padding: 8px 14px;
      border-radius: 8px;
      cursor: pointer;
      font-size: 14px;
    }

    .btn:hover {
      opacity: 0.92;
    }

    .code-block {
      background: #0f1722;
      color: #d9e7ff;
      padding: 16px;
      border-radius: 12px;
      font-family: Consolas, monospace;
      white-space: pre-wrap;
      overflow-wrap: anywhere;
      word-break: break-word;
      overflow-x: auto;
      overflow-y: auto;
      line-height: 1.55;
      max-height: 420px;
      font-size: 13px;
      box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.04);
    }

    .code-card,
    .code-block,
    .pipeline-card,
    .workflow-item,
    .pdf-card,
    .image-card {
      min-width: 0;
    }

    .code-card h3,
    .code-description,
    .code-top a,
    .script-name,
    .image-caption a,
    .pdf-card h3,
    .workflow-item h3,
    .block p,
    .block li {
      overflow-wrap: anywhere;
      word-break: break-word;
    }

    @media (max-width: 1100px) {
      .pipeline-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 900px) {
      .image-grid,
      .workflow-grid,
      .pipeline-grid {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 640px) {
      .block {
        padding: 18px 16px;
        border-radius: 14px;
      }

      .block h2 {
        font-size: 22px;
        line-height: 1.2;
      }

      .block h3,
      .code-card h3,
      .workflow-item h3,
      .pdf-card h3 {
        font-size: 17px;
        line-height: 1.3;
      }

      .code-card,
      .pipeline-card,
      .workflow-item,
      .pdf-card {
        padding: 16px;
        border-radius: 12px;
      }

      .code-top {
        align-items: stretch;
      }

      .btn {
        width: 100%;
      }

      .code-block {
        font-size: 11px;
        padding: 12px;
      }
    }
  </style>

  <!-- 1. Description / context -->
  <div class="block">
    <h2 id="intro-heading">{translations.ru.caseLabel}</h2>
    <div class="intro-text" id="intro-text">{translations.ru.caseIntro}</div>

    <h3 class="findings-title" id="findings-heading">{translations.ru.caseResultsTitle}</h3>

    <ul class="findings-list" id="findings-list">
      {translations.ru.caseResults.map((item) => (
        <li>{item}</li>
      ))}
    </ul>
  </div>

  <!-- 2. Maps -->
  <div class="block">
    <h2 id="maps-heading">{translations.ru.maps}</h2>

    <div class="image-grid">
      {images.map((item, index) => (
        <div class="image-card">
          <a href={item.src} target="_blank" rel="noopener noreferrer">
            <img src={item.src} alt={`niamey-image-${index + 1}`} />
          </a>
          <div class="image-caption">
            <a href={item.src} target="_blank" rel="noopener noreferrer" id={`image-title-${index}`}>
              {translations.ru.imageTitles[index]}
            </a>
          </div>
        </div>
      ))}
    </div>
  </div>

  <!-- 3. PDFs -->
  <div class="block">
    <h2 id="pdf-heading">{translations.ru.pdf}</h2>

    <div class="pdf-grid">
      {pdfs.map((pdf, index) => (
        <div class="pdf-card">
          <h3 id={`pdf-title-${index}`}>{translations.ru.pdfTitles[index]}</h3>
          <a href={pdf.file} target="_blank" rel="noopener noreferrer" class="pdf-open">
            {translations.ru.pdfOpen}
          </a>
        </div>
      ))}
    </div>
  </div>

  <!-- 4. Code blocks -->
  <div class="block">
    <h2 id="python-heading">{translations.ru.python}</h2>

    <div class="code-grid">
      <div class="code-card">
        <h3 id="code-title-1">{translations.ru.codeTitle1}</h3>

        <div class="code-description" id="code-desc-1">{translations.ru.codeDesc1}</div>

        <div class="code-top">
          <a
            href="/assets/niamey/code/niamey_slope.py"
            target="_blank"
            rel="noopener noreferrer"
            class="code-open"
          >
            {translations.ru.fileOpen}
          </a>
          <button type="button" class="btn copy-btn" onclick="copyCode('code1', this)">
            {translations.ru.copy}
          </button>
        </div>

        <pre class="code-block" id="code1">Загрузка...</pre>
      </div>

      <div class="code-card">
        <h3 id="code-title-2">{translations.ru.codeTitle2}</h3>

        <div class="code-description" id="code-desc-2">{translations.ru.codeDesc2}</div>

        <div class="code-top">
          <a
            href="/assets/niamey/code/niamey_flow.py"
            target="_blank"
            rel="noopener noreferrer"
            class="code-open"
          >
            {translations.ru.fileOpen}
          </a>
          <button type="button" class="btn copy-btn" onclick="copyCode('code2', this)">
            {translations.ru.copy}
          </button>
        </div>

        <pre class="code-block" id="code2">Загрузка...</pre>
      </div>
    </div>
  </div>

  <!-- 5. Workflow -->
  <div class="block">
    <h2 id="workflow-heading">{translations.ru.workflow}</h2>

    <div class="workflow-grid">
      {translations.ru.workflowItems.map((wf, wfIndex) => (
        <div class="workflow-item">
          <h3 id={`workflow-title-${wfIndex}`}>{wf.title}</h3>
          <ul id={`workflow-steps-${wfIndex}`}>
            {wf.steps.map((step) => (
              <li>{step}</li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>

  <!-- 6. Full pipeline -->
  <div class="block">
    <h2 id="pipeline-heading">{translations.ru.pipeline}</h2>
    <div class="intro-text" id="pipeline-intro">{translations.ru.pipelineSubtitle}</div>

    <div class="pipeline-grid">
      {pipelineScripts.map((script) => (
        <div class="pipeline-card">
          <p class="script-name">{script.name}</p>
          <a href={script.file} target="_blank" rel="noopener noreferrer" class="script-open">
            {translations.ru.scriptOpen}
          </a>
        </div>
      ))}
    </div>
  </div>

  <script define:vars={{ translations, codeFetchTargets }}>
    const i18n = translations;
    const LANG_KEY = "lang";

    /** [element id, key on current locale bundle] */
    const staticTextFields = [
      ["intro-heading", "caseLabel"],
      ["intro-text", "caseIntro"],
      ["findings-heading", "caseResultsTitle"],
      ["maps-heading", "maps"],
      ["pdf-heading", "pdf"],
      ["workflow-heading", "workflow"],
      ["python-heading", "python"],
      ["pipeline-heading", "pipeline"],
      ["pipeline-intro", "pipelineSubtitle"],
      ["code-title-1", "codeTitle1"],
      ["code-title-2", "codeTitle2"],
      ["code-desc-1", "codeDesc1"],
      ["code-desc-2", "codeDesc2"]
    ];

    function setText(id, text) {
      const el = document.getElementById(id);
      if (el) el.textContent = text;
    }

    function fillListElement(id, items) {
      const root = document.getElementById(id);
      if (!root) return;
      root.replaceChildren(
        ...items.map((text) => {
          const li = document.createElement("li");
          li.textContent = text;
          return li;
        })
      );
    }

    function applyIndexedTitles(prefix, titles) {
      titles.forEach((title, index) => {
        setText(`${prefix}-${index}`, title);
      });
    }

    function applyNiameyCopy(lang) {
      const t = i18n[lang];
      if (!t) return;

      for (const [id, key] of staticTextFields) {
        setText(id, t[key]);
      }

      fillListElement("findings-list", t.caseResults);

      t.workflowItems.forEach((wf, wfIndex) => {
        setText(`workflow-title-${wfIndex}`, wf.title);
        fillListElement(`workflow-steps-${wfIndex}`, wf.steps);
      });

      applyIndexedTitles("image-title", t.imageTitles);
      applyIndexedTitles("pdf-title", t.pdfTitles);

      document.querySelectorAll(".pdf-open").forEach((el) => {
        el.textContent = t.pdfOpen;
      });
      document.querySelectorAll(".code-open").forEach((el) => {
        el.textContent = t.fileOpen;
      });
      document.querySelectorAll(".script-open").forEach((el) => {
        el.textContent = t.scriptOpen;
      });
      document.querySelectorAll(".copy-btn").forEach((btn) => {
        btn.textContent = t.copy;
        btn.dataset.copyLabel = t.copy;
        btn.dataset.copiedLabel = t.copied;
      });
    }

    async function loadCode(id, path) {
      const el = document.getElementById(id);
      if (!el) return;
      try {
        const res = await fetch(path);
        if (!res.ok) throw new Error("fetch failed");
        el.textContent = await res.text();
      } catch {
        const lang = localStorage.getItem(LANG_KEY) || "ru";
        el.textContent = i18n[lang]?.codeLoadError ?? i18n.ru.codeLoadError;
      }
    }

    window.copyCode = function (id, btn) {
      const el = document.getElementById(id);
      if (!el) return;
      const text = el.textContent;
      navigator.clipboard.writeText(text);

      const copiedLabel = btn.dataset.copiedLabel || "Скопировано";
      const copyLabel = btn.dataset.copyLabel || "Копировать";

      btn.textContent = copiedLabel;
      setTimeout(() => {
        btn.textContent = copyLabel;
      }, 1200);
    };

    function initNiameyPage() {
      applyNiameyCopy(localStorage.getItem(LANG_KEY) || "ru");

      window.addEventListener("languageChanged", (event) => {
        applyNiameyCopy(event.detail.lang);
      });

      for (const { id, path } of codeFetchTargets) {
        loadCode(id, path);
      }
    }

    initNiameyPage();
  </script>
</SiteLayout2>
